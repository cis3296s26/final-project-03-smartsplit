// Central place for backend URLs.
// API_BASE is the prefixed base used by /api/* routes (e.g. /api/households, /api/expenses).
// API_ROOT is the root used by older non-prefixed routes that still exist in the backend
// (e.g. /household/create, /household/join, /household/payments, etc.).

export const API_BASE = "http://127.0.0.1:5001/api";
export const API_ROOT = "http://localhost:5001";
